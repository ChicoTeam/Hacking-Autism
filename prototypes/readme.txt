These prototypes are meant to be instantiated as objects. 

Example usage:

// load Page with id of 1
var myPage = new Page(1);

myPage.setDescription("my page");

// save page to database
myPage.save();
